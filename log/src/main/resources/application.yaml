eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/
  instance:
    instance-id: logApp
    prefer-ip-address: true

server:
  port: 8007
spring:
  application:
    name: log-proj